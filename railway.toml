[build]
builder = "NIXPACKS"
buildCommand = "cd web && npm install && npm run build"

[deploy]
startCommand = "cd web && npm run start -- -p $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
